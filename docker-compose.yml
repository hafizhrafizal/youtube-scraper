services:
  app:
    build: .
    container_name: youtube-scraper
    restart: unless-stopped
    env_file: .env
    networks:
      - app_net

networks:
  app_net:
    external: true
    name: app_default

